name: Deploy Temporary Website Preview

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out preview repository
        uses: actions/checkout@v6

      - name: Download generated static website
        run: |
          curl --fail --location --retry 3 \
            'https://sdmntprpolandcentral.oaiusercontent.com/files/00000000-abd4-820a-880c-fe43712eb8f0/raw?se=2026-08-03T21:34:38Z&sp=r&sv=2026-02-06&sr=b&scid=c3853ef5-e1f3-54ec-9829-09ca8ef52f9a&skoid=e5f84a89-964a-4fd3-92c2-c2cb3a12ea16&sktid=a48cca56-e6da-484e-a814-9c849652bcb3&skt=2026-08-03T19:23:44Z&ske=2026-08-04T19:23:44Z&sks=b&skv=2026-02-06&sig=TEtFDFr2U4f/5mR9YCN5Mv7m0FMnZ8tEC1d7k8L8KQo%3D' \
            --output preview.zip
          mkdir site
          unzip -q preview.zip -d site

      - name: Validate public preview boundary
        run: |
          test -f site/index.html
          grep -q 'noindex, nofollow' site/index.html
          if grep -R -i -E 'company os|api[_ -]?key|secret|password' site --include='*.html' --include='*.css' --include='*.xml' --include='*.txt'; then
            echo 'Forbidden internal content found in public preview.'
            exit 1
          fi

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload static website
        uses: actions/upload-pages-artifact@v4
        with:
          path: site

  deploy:
    needs: validate
    runs-on: ubuntu-24.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy preview
        id: deployment
        uses: actions/deploy-pages@v4
